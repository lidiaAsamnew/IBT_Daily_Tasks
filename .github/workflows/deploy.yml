name: Deploy Addis Eat

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload Addis Eat
        uses: actions/upload-pages-artifact@v4
        with:
          path: './Module2/Day23-25/Addis Eat'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4